import NavBar from "../NavBar/NavBar";
import Logo from "../Logo/Logo";
import "./Header.scss";

const Header = () => {
  return (
    <header>
      <Logo />
      <NavBar />
    </header>
  );
};

export default Header;
